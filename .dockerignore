# Каталоги сборки — сотни мегабайт, которые контексту не нужны и которые всё равно
# пересобираются внутри образа. Без этого `docker build` сначала переливает их целиком.
build/
*/build/
.gradle/
.kotlin/
.git/
.idea/
api/
docs/
.DS_Store
